spring:
  application:
    name: kontrolBankData

  thymeleaf:
    cache: false

  config:
    import: optional:file:./config/database.yaml

  sql:
    init:
      mode: never

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
